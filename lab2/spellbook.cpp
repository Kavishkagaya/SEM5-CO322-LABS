#include <iostream>

using namespace std;

int main()
{
    int p, q;
    cin >> p >> q;

    int l[p], m[q];

    for (int i = 0; i < p; i++)
    {
        cin >> l[i];
    }

    for (int i = 0; i < q; i++)
    {
        cin >> m[i];
    }

    for (int i = 0; i < p; i++)
    {
        for (int i = 0; i < q; i++)
        {
        }
    }

    return 0;
}